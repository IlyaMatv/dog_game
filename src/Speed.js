import React from 'react'

const Speed = (props) => {

    return (
        <div style={{marginTop: '20px'}}>
            Speed is : {props.speed}
        </div>
    )
}

export default Speed